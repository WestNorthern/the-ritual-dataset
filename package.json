{
  "name": "the-ritual-dataset",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "echo 'add eslint later'"
  },
  "devDependencies": {
    "@types/node": "^20"
  }
}